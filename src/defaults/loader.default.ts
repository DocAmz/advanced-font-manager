import { FontLoaderOptions } from "../types/loader";

export const DEFAULT_LOADER_OPTIONS: FontLoaderOptions = {
  useResolvers: false,
  disableWarnings: false,
  useCache: false,
  enableDebug: false,
}

